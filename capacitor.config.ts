import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.todoApp.app',
  appName: 'todoApp',
  webDir: '../BackEndProject/src/main/resources/static'
};

export default config;
